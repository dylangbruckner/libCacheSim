generate a series of caching algorithms in the same format as the plugin_fifo.py and plugin_seive.py and run them on several cahing bencmarks that can be found online. They should have the same hooks and general file format, and expose the same behavior. The two existing files should be treated as correct implementations, and any caches should be built similarly. The goal is to optimize perforance. Start by implement S3FIFO, then try to implement hybrids of seive and S3FIFO, then go through old papers and examine other strategies. Report the 3 best caching algorithms on the existing benchmark as well as other benchmarks that you include. 