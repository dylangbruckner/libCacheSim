I have used you to generate a series of caching algorithms and benchmarks for my assignments, the instructions for which are in instructions.txt. Now I'm trying to improve my score to at least 85. I found that S3FIFO is having the best results so far. See notes.txt for the full results of everything that I've tried. Try to test a bunch of different ghost cache and small cache size combinations first. Then, try implementing other queue types in the main queue of S3 other than FIFO, and see how they compare. If that doesn't work, keep in mind that metadata storage in this is relatively unbounded, so we can store information on all seen objects. Is there a way we could create a cache which dynamically detects how many previously seen items are seen per total item, then use that plus metadata of when we've last seen an object to determine whether to cache it or not? idk what the evictino strategy woould be, so it would be helpful to test some different ones, but it could also utilize a small queue like s3 to cache all incoomoing requests at least temporarily